# @touchstone/hooks

Small, dependency-free React hooks used by Touchstone components. Safe for consumers to import directly when they need the same primitives.

```ts
import { useMergedRefs, useControllableState } from '@touchstone/hooks';
```
