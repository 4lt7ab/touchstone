# @touchstone/storybook

Storybook 8 (Vite framework) for every component in the workspace.

```bash
pnpm storybook
```

It picks up stories from `packages/*/src/**/*.stories.tsx`. The toolbar theme switcher toggles `lightTheme` ↔ `darkTheme` on `<body>` via `@storybook/addon-themes`.
