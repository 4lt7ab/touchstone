# @touchstone/storybook

Storybook 8 (Vite framework) for every component in the workspace.

```bash
pnpm storybook
```

It picks up stories from `packages/*/src/**/*.stories.tsx`. The toolbar theme switcher cycles every Touchstone theme on `<body>` via `@storybook/addon-themes`, defaulting to `warmSandTheme`.
